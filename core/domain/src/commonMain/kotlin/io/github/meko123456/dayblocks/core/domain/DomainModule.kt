package io.github.meko123456.dayblocks.core.domain

import org.koin.dsl.module

/**
 * Use-case bindings. Empty while the skeleton is being stood up; the models, repository
 * interfaces and use cases land in step 2 and register here.
 */
val domainModule = module {
}
